import { useState } from "react";
import Button from "@mui/material/Button";

export const EditProjectButton = ({ project, projectId }) => {
  return (
    <>
      <Button variant="contained">Edit Project</Button>
    </>
  );
};
